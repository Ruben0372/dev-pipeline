#!/bin/bash
# Auto-push to origin after every commit.
# Runs in background to avoid blocking the committer.
BRANCH=$(git branch --show-current 2>/dev/null || echo "main")
(git push origin "$BRANCH" &>/dev/null &)
