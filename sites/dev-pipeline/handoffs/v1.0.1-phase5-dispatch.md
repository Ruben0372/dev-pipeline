# Phase 5 Handoff: Dispatch & Execution Engine

**Date:** 2026-03-26
**Issues:** #15, #16
**Status:** Complete (executor is stub — Claude CLI prereq pending)

## What Was Done

### 5.1 Dispatch Integration
- DB migration 11: `source_issue_id TEXT`, `approval_status TEXT DEFAULT 'auto'` on dispatches
- Updated Dispatch model with SourceIssueID and ApprovalStatus fields
- DispatchStore: GetBySourceIssue() for dedup, UpdateApprovalStatus(), expanded Create()
- IssueScanner service: scans sites/*/issues/*.md for status:open, creates dispatches
- Severity routing: critical/high auto-approved, medium/low needs_approval
- Wired into Scheduler: runs after every git pull
- PUT /api/dispatch/{id}/approve and PUT /api/dispatch/{id}/reject endpoints

### 5.2 Execution Engine (Stub)
- DispatchExecutor service created with full framework
- Processes approved+queued dispatches one at a time (mutex)
- Max 3 retries per issue with tracking
- WebSocket broadcasts: dispatch_progress, dispatch_completed
- Activity logging on completion
- Claude CLI invocation is a stub — needs Claude Code installed on Arch (Phase 5.2 prereq)

## API Endpoints Added

| Method | Path | Purpose |
|--------|------|---------|
| PUT | /api/dispatch/{id}/approve | Approve needs_approval dispatch |
| PUT | /api/dispatch/{id}/reject | Reject dispatch, mark as failed |

## Next Steps for Full Execution

1. Install Claude Code CLI on Arch server
2. Replace stub in dispatch_executor.go execute() with: `claude --prompt <prompt> --project <path>`
3. Wire DispatchExecutor.ProcessApproved() into DispatchService.processQueue()
