# Phase 1 Handoff: Reliability Foundation

**Date:** 2026-03-26 **Issue:** #18 **Status:** Complete

## What Was Done

### 1.1 Broken Window Gate

* Fixed `TestNew_SchemaVersionIncremented`: updated expected version from 9 to 10 (11 migrations exist, 0-10)

* Fixed `Layout.test.tsx`: breadcrumb tests now match individual segment nodes instead of the full composite string

* All Go tests (19) and web tests (21) pass with zero failures

* Added `make test-gate`: runs full Go + web test suite, exits non-zero on any failure

* Added `make ship`: depends on `test-gate`, builds containers, then runs smoke check

### 1.2 Post-Deploy Smoke Check

* Created `.smoke` file with health endpoint (200 + "ok") and UI endpoint (200 + "tower")

* Added `make smoke`: reads `.smoke`, curls each URL, checks status code + body substring

* Wired into `make ship` and `make deploy`: smoke runs after container startup

* Added smoke endpoints section to `sites/dashboard/operations/deployment.md`

### 1.3 Docker Build Cache Warming

* Created `scripts/cache-warm.sh`: pulls base images (golang, node, nginx, alpine), warms dashboard build cache

* Includes retry-once-on-failure logic for TLS flakes

* Added Alpine CDN mirror fallback to `api/Dockerfile`

* Script ready for Tower scheduled task (daily 04:00)

## Files Changed

### Dashboard repo

* `api/internal/db/db_edge_test.go` — schema version fix

* `web/src/__tests__/Layout.test.tsx` — breadcrumb test fix

* `Makefile` — added test-gate, ship, smoke, deploy targets

* `.smoke` — smoke endpoint definitions

* `api/Dockerfile` — Alpine mirror fallback

* `CLAUDE.md` — issue reporting convention

### Dev-pipeline repo

* `scripts/cache-warm.sh` — Docker cache warming

* `sites/dashboard/operations/deployment.md` — smoke endpoints section

## Acceptance Criteria Status

* [x] `make ship` blocks on test failures (zero broken windows)
* [x] `make smoke` validates UI + API after deploy
* [ ] Docker builds on Arch succeed without TLS flakes for 7 days (monitoring period started)

## Next Phase

Phase 3 (Pipeline Automation) depends on Phase 1 being complete. Ready to proceed.
